{
  "name": "exif-importer",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "exif-importer": "./bin/exif-importer"
  },
  "scripts": {
    "test": "node test/test.mjs",
    "lint": "eslint --fix .",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "description": "CLI tool for generating XMP metadata files from video files",
  "dependencies": {
    "date-fns": "^4.1.0",
    "dist-exiftool": "^10.53.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.0.1",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
